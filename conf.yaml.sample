server:
  listen_addr: localhost
  port: 3000

  logging:
    app:
      console:
        level: info
    express:
      console:
        level: info

database:
  host: host.vnet
  username: expapp
  password: expapp
  database: expapp
  dialect: postgres
